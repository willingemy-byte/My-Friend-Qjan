name: AIV V20 Android build

on:
  workflow_dispatch:
  push:
    branches: [aiv-v20-build]
    paths:
      - "aiv-v19/**"
      - ".github/workflows/aiv-v20-build.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache Android toolchain
        uses: actions/cache@v4
        with:
          path: aiv-v19/.toolchain
          key: aiv-v20-android35-ndk27d

      - name: Build V20 unsigned APK
        working-directory: aiv-v19
        env:
          AIV_BUILD_MODE: unsigned
        run: |
          chmod +x bootstrap-clean-build.sh
          ./bootstrap-clean-build.sh

      - name: Verify V20 artifact
        run: |
          test -f aiv-v19/build/AIV-0.6.20-unsigned.apk
          test -f aiv-v19/build/AIV-0.6.20-unsigned.apk.sha256
          cat aiv-v19/build/AIV-0.6.20-unsigned.apk.sha256

      - name: Upload V20 APK
        uses: actions/upload-artifact@v4
        with:
          name: AIV-0.6.20-unsigned-test
          path: |
            aiv-v19/build/AIV-0.6.20-unsigned.apk
            aiv-v19/build/AIV-0.6.20-unsigned.apk.sha256
          retention-days: 30

      - name: Upload verified Android signing tool
        uses: actions/upload-artifact@v4
        with:
          name: AIV-0.6.20-signing-tool
          path: aiv-v19/.toolchain/sdk/build-tools/35.0.0/lib/apksigner.jar
          retention-days: 30
